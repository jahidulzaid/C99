#include <stdio.h>
int main(){
int num, original, remainder, i = 0;
printf("Enter a Number: ");
scanf("%d", &num);
original = num;


for(original = num;original != 0; ++i){
    original /= 10;
}
for(original = num; original != 0; original /= 10)
    scanf("%d", &a);




if (flag == 0)
    printf("%d is a prime number,", n);
else
    printf("%d is not a prime number,", n);
}

